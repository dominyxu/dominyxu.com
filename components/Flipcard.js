import { useState } from "react";

function Flipcard({pic, role, location, dates, description}){

    const [flip, setFlip] = useState(false);

    return(
        <div>
          
        </div>
    );
}
